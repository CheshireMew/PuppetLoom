# 验收记录

日期：2026-08-15
平台：Windows，RTX 2080 Super

## 自动链

| 检查 | 结果 |
| --- | --- |
| 严格 TypeScript 检查 | core、renderer、cli、desktop 全部通过 |
| 构建 | 四个工作区全部通过 |
| 单元与集成 | 8 个测试文件、69 项测试全部通过，包含双表面头部、眼眶共用固定点、远近眼透视、脸颊与下巴转向、头颈连续连接、上半身转向与脚部固定、自然闭眼收拢、三态嘴形淡化、偶发完整开合、冻结主运动后的持续风动、呆毛二维弹跳、头饰与耳朵分向、前后发反相响应、尾根固定、素材参考裁切、视线回中、中立还原和逐帧平滑检查 |
| Electron 用户链 | 创建、透明窗口、WebGL2、缩放、暂停、置顶、鼠标穿透恢复和重复启动复用同一窗口通过 |
| WebGL 视觉检查 | 透明像素 56.96%，可见像素 42.82%，相隔 1.8 秒两帧变化 10.78% |
| 1280×1280/23 层性能 | 平均 60.00 FPS，95 分位 16.8 ms，WebGL2 可用 |

性能数值是本次验收记录，不作为其它硬件的保证；自动脚本仍按 `docs/TESTING.md` 中的阈值重新测量。

## 本机开源样例

以下素材只从用户已经下载的压缩包解出到 `D:\Tools\PuppetLoomIntegration\run-20260814-2115`，没有提交到 PuppetLoom：

| 来源 | 输入 | 识别结果 | 最终结果 |
| --- | --- | --- | --- |
| Anime2.5DRig | 1280×1280、22 个可见图层 | `grouped` | 13 姿态通过、纹理齐全、透明窗口可运行 |
| AutoLive2d | 768×768、20 个可见图层 | `semantic` | 13 姿态通过、纹理齐全、透明窗口可运行 |

两者的安全缩放均为 `1`。这项检查证明外部 PSD 能走通导入、创建、报告、验证和真实 Electron 播放链；它不替代新角色原图与对应 See-through PSD 的最终视觉验收。

## 当前角色的本机复验

再次修复 `headwear` 后的 See-through PSD 为 1280×1280、24 个可见图层，24 层全部识别。r20 完全从新版 PSD 重建，没有复用旧头饰纹理；`coherent-v2` 保持 `semantic` 绑定和完整动作范围，13 个姿态复验均得分 `1`。接入闭眼与三态嘴形后共 29 个运行图层，自动眨眼和偶发嘴部开合均启用。五档转头图现在包含延迟后的上半身姿态；同一眼眶的图层保持对齐，脖子上端跟头、下端跟衣领，脸、前发、后发和上身没有在 `0.85` 姿态断开。

26 秒完整时间线测得：头饰与耳尖最大 `4.394 px`、后发自由端 `25.346 px`、前发与呆毛 `17.891 px`、裙摆 `6.533 px`、上衣 `0.625 px`、尾端 `21.248 px`。另把头部、身体、视线、呼吸和表情全部冻结 12 秒后，纯次级运动仍测得前发与呆毛约 `15.13 px`、后发约 `7.89 px`、耳朵与头饰两侧约 `3.42 px`、裙摆约 `3.12 px`、尾端约 `15.76 px`；前后发使用不同相位，脚部不参与身体摆动。角色项目和素材只保存在忽略的本机 `workspace`，没有提交到 Git。
