# 验收记录

日期：2026-08-14  
平台：Windows，RTX 2080 Super

## 自动链

| 检查 | 结果 |
| --- | --- |
| 严格 TypeScript 检查 | core、renderer、cli、desktop 全部通过 |
| 构建 | 四个工作区全部通过 |
| 单元与集成 | 6 个测试文件、47 项测试全部通过，包含统一姿态场对称性、中立还原和逐帧平滑检查 |
| Electron 用户链 | 创建、透明窗口、WebGL2、缩放、暂停、置顶和鼠标穿透恢复通过 |
| WebGL 视觉检查 | 透明像素 56.94%，可见像素 42.85%，相隔 1.8 秒两帧变化 6.55% |
| 1280×1280/23 层性能 | 平均 60.00 FPS，95 分位 17.1 ms，WebGL2 可用 |

性能数值是本次验收记录，不作为其它硬件的保证；自动脚本仍按 `docs/TESTING.md` 中的阈值重新测量。

## 本机开源样例

以下素材只从用户已经下载的压缩包解出到 `D:\Tools\PuppetLoomIntegration\run-20260814-2115`，没有提交到 PuppetLoom：

| 来源 | 输入 | 识别结果 | 最终结果 |
| --- | --- | --- | --- |
| Anime2.5DRig | 1280×1280、22 个可见图层 | `grouped` | 13 姿态通过、纹理齐全、透明窗口可运行 |
| AutoLive2d | 768×768、20 个可见图层 | `semantic` | 13 姿态通过、纹理齐全、透明窗口可运行 |

两者的安全缩放均为 `1`。这项检查证明外部 PSD 能走通导入、创建、报告、验证和真实 Electron 播放链；它不替代新角色原图与对应 See-through PSD 的最终视觉验收。

## 当前角色的本机复验

修复后的 See-through PSD 为 1280×1280、24 个可见图层，24 层全部识别。使用 `coherent-v1` 创建后保持 `semantic` 绑定和完整动作范围，13 个姿态得分均为 `1`，纹理齐全；唯一禁用项是缺少闭眼素材导致的眨眼。20 秒自主运动预览同时输出全身与头部裁切，确认中立帧保持原合成、转头由统一姿态场驱动、脚部不参与呼吸。该角色项目和素材只保存在忽略的本机 `workspace`，没有提交到 Git。
