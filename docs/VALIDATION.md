# 验收记录

## 2026-08-17 Alpha 轮廓 ArtMesh

| 检查 | 结果 |
| --- | --- |
| 网格形状 | 非矩形透明图层按 Alpha 外轮廓、透明孔洞和断开的独立区域生成受约束三角网格；完全不透明矩形与 v1-v3 历史项目继续使用规则网格。前发实测网格不再覆盖整张矩形纹理，刘海开口和两侧发束均沿真实可见区域收边。 |
| 编辑与重建 | 编辑器显示网格类型、细节尺度、区域数、孔洞数、顶点数和三角形数。重建后按 UV 把七通道逐顶点权重投影到新网格，软选择距离沿三角形邻接传播，不跨透明断层影响另一块部件。 |
| 格式兼容 | 项目格式升级为 v4；v1-v3 在内存中补为 `grid` 拓扑，v3 authoring 数据保持不变。ArtMesh 保存可重建的纹理尺寸、Alpha 阈值、细节尺度和轮廓区域，不依赖重新读取 PSD。 |
| 自动验证 | `npm run test:unit` 完成 19 个测试文件、131 项测试，其中 4 项专门覆盖断开区域与孔洞、三角形落点、重建与权重投影、矩形回退和软选择隔离。`npm run test:e2e` 通过并保存 `editor-art-mesh.png` 作为真实 Electron 编辑器证据。 |
| 视觉与性能 | 23 层性能样本由 11087 个顶点降到 3098 个；Alpha 外缘精度保持不变，内部三角形独立稀疏。1280×1280、WebGL2 实测平均 59.999 FPS，95 分位帧时 17.3 ms，达到既有门槛。 |

## 2026-08-17 Windows 一体化窗口外壳

| 检查 | 结果 |
| --- | --- |
| 创建边界 | 创建器与编辑器共用的控制窗口在唯一 BrowserWindow 创建点显式使用 `frame: false`；最小化、最大化、还原、关闭和状态读取由主进程持有，渲染层只呈现 `WindowTitleBar` 并调用受限 IPC。 |
| 原生整窗证据 | `desktopCapturer` 通过目标 BrowserWindow 的 `getMediaSourceId()` 捕获 Windows 整窗，不再把 Playwright 内容截图当成外壳证据。最新启动器和编辑器捕获均为 1440×900 逻辑尺寸、2 倍缩放下 2880×1800 像素；两者的外框与内容边界完全相同，人工查看 `launcher-native.png` 与 `editor-native.png` 未见 Windows 原生标题栏。 |
| 真实窗口动作 | 自动链实际拖动标题栏，完成最大化、A→B→A 还原、最小化与恢复，并通过自定义关闭按钮进入正常 BrowserWindow 关闭流程；关闭后读取到最后一次编辑器草稿，确认没有绕过原有持久化拦截。 |
| 证据时序 | 原生整窗捕获前等待连续两次浏览器绘制，且先取整窗证据、再取 Playwright 内容截图，避免全页截图临时改变渲染状态后留下标题栏残缺的假证据。窗口状态、原生来源、边界、缩放与所有截图路径统一写入 `test/artifacts/runs/mswtudpf-dx0-40d981e0/window-shell-evidence.json`。 |
| 自动验证状态 | `npm run typecheck -w @puppetloom/desktop` 与 `npm run build -w @puppetloom/desktop` 通过。`npm run test:e2e` 已完成窗口、编辑器、透明角色窗口和关窗草稿用户链并写出上述证据；整条命令随后仍因同一 semantic fixture 被当前并行 core 改动从预期 `semantic` 识别为 `grouped` 而失败。该断言保持原样，未用放宽分类契约换取绿色结果。 |

## 2026-08-17 Cubism 官方格式桥接

| 检查 | 结果 |
| --- | --- |
| 格式边界 | `.moc3` 只接受 Cubism Editor 官方导出，不实现私有二进制写入器；`model3/exp3/motion3/physics3/cdi3` 使用公开 JSON 结构。 |
| 参数映射 | 11 个 PuppetLoom 标准语义映射为 12 个 Cubism 标准参数；眨眼拆为左右眼并按 `1 - value` 反向换算，自定义参数使用稳定 ID。 |
| Editor 链 | WebSocket 注册兼容 1.1.0 与 0.9.5；Allow、Edit、模型 UID、建模模式、参数和对象检查已实现。所有编辑位于一个 `EditBegin/EditEnd` 事务，失败测试确认发送 `Cancel: true`。 |
| 不可写边界 | ArtMesh 顶点、Warp 控制点、平移和非等比缩放等不能由当前官方 API 完整表达的内容进入 blocking issue；默认严格同步在开始事务前停止，只有显式 `--allow-partial` 才同步剩余结构。 |
| 侧车与最终目录 | expression、motion、physics 和 display-info 可生成；finalize 保留 Editor 原引用，合并现有 physics/cdi、追加 PuppetLoom expression/motion，再在旁路目录验证后发布。 |
| 自动验证 | 新增 7 个 core 测试和 1 个 CLI 端到端场景，覆盖 WebSocket 信封、映射、阻断、曲线计数、5.3/5.4 API 边界、提交/回滚、MOC3 文件头、纹理和 `plan → prepare → finalize → verify`。 |
| 官方样例 | 从 Live2D 官方 `CubismWebSamples` 稀疏检出的 Mao 资源位于 `D:\Tools\PuppetLoom-cubism-qa`，原始 model3 的 moc、纹理、pose、physics、8 个 expression 和 9 个 motion 引用全部通过；与本机 r34 合并后的新目录也通过，不改源项目或官方样例。 |

本机 Cubism Editor 5.3.03 可执行检查、临时参数预览、清除预览、格式验证和 Viewer 打开。External API 1.1.0 结构同步需要 5.4 alpha 或后续版本；本轮没有替用户下载、安装或接受 alpha 许可，因此真实 Editor 编辑事务由模拟官方协议测试覆盖，尚未在本机 5.4 实例执行。结构验证通过不等于视觉等价，当前 r34 仍因程序化逐顶点变形被正确标记为 `strictReady: false`。

## 2026-08-17 项目治理复验

本轮复验在 Windows 上直接使用当前源码构建，没有使用仓库中旧的 `dist` 作为验收对象。

| 检查 | 结果 |
| --- | --- |
| 当前源码闭环 | `npm run typecheck` 通过；`npm test` 先构建 core、renderer、cli 和 desktop，再完成 21 个测试文件、165 项测试。 |
| AI authoring 链 | v3 标准/自定义参数、一维/二维关键形态、旋转/网格变形器、表情、参数物理和行为均通过求值与结构测试；`author inspect/apply` 通过真实 CLI 完成 revision 提交、自动端点证据和关闭重开，过期/依赖/循环边界由同一事务拒绝。 |
| 兼容与交付 | 本机旧角色不改写原文件即可获得 11 个标准语义参数，29 层、13 姿态、安全系数 1.00 全部通过；`export` 把有效 authoring 修订烘焙为 revision 0 新目录，重新验证并拒绝覆盖既有目标。 |
| 持久化边界 | 并发同基线保存只有一个能提交；证据生成失败不会推进 revision；创建失败不会暴露部分项目；纹理、来源、网格、修订链和证据哈希的损坏都能被验证器拒绝。 |
| 渲染契约 | 离线证据与 WebGL 共用图层顺序、透明度和六种混合规则；Alpha 剪裁使用纹理掩码；所有证据产物记录 SHA-256。 |
| Electron 用户链 | `npm run test:e2e` 通过。实测键盘微调、有未保存草稿时拒绝恢复、真实 BrowserWindow 关闭前刷新草稿，以及编辑器与透明角色窗口；真实角色 E2E 另确认 AI Authoring 检查面板可见。 |
| Windows 启动链 | `npm run test:launcher` 通过，运行日志策略先写入，启动和正常退出事件完整。 |
| WebGL 视觉与性能 | `npm run test:visual` 通过：透明像素 57.37%、可见像素 42.46%、姿态变化 13.22%。`npm run test:performance` 通过：平均 60.006 FPS、95 分位 17.3 ms、WebGL2 可用。 |
| 动态证据 | `npm run test:motion-evidence` 通过。自主运动最大帧差 11.41%，冻结头部与身体后的次级运动最大帧差 1.05%，非方形视口保持 1.5:1；清单收录全部视频、接触表、报告和项目文件的大小与 SHA-256。 |
| 产物治理 | 每次测试运行在写入任何负载前先做预估容量、总预算和剩余空间检查，并先写 `run.json`；跨进程预检锁会计入活动峰值预留，两个并发任务不能分别通过检查后共同越过总预算。结束时记录 succeeded/failed/interrupted、可复用性判断、分类字节数、峰值口径和完整性清单。预算拒绝、并发预留、受控失败和中断恢复均有自动测试，治理工具只报告候选项，不自动删除。 |

本轮还执行了托管模板漂移验证和生产存储复审，两者均通过。CI 已使用 Windows 执行同样的当前源码类型检查和测试链。

日期：2026-08-16
平台：Windows，RTX 2080 Super

## 自动链

| 检查 | 结果 |
| --- | --- |
| 严格 TypeScript 检查 | core、renderer、cli、desktop 全部通过 |
| 构建 | 四个工作区全部通过 |
| 单元与集成 | 15 个测试文件、125 项测试全部通过。除原有姿态、控制笼、透视、结构连接、次级运动、素材与 CLI 检查外，覆盖项目格式 v2、统一安全消费边界、草稿生命周期、图层层级/顺序/可见性/锁定、网格密度重采样、软选择数据、脸部/头骨/头部/身体/视线/次级运动逐顶点权重、分部响应、透明区域拓扑、完整顶点描述、PSD 安全迁移、修订恢复、用户证据、九姿态/九运动、前后比较和编辑器启动契约。 |
| Electron 用户链 | 从最近项目进入编辑器，切换图层显示/锁定，软选择拖动多个网格顶点，设置分部响应，自动保存草稿，返回主页并重开恢复，撤销/重做，保存 revision，检查修改前/修改后/分割/叠加/差异五种证据并确认会话，再从编辑器打开透明窗口；光标目标、跟随、缩放、暂停、置顶、穿透恢复、重复启动复用和非等比窗口不拉伸全部通过。 |
| Windows 启动入口 | `启动PuppetLoom.cmd` 所调用的 PowerShell 5.1 兼容脚本实际启动 Electron，隔离配置日志记录 `app-ready` 与正常退出。 |
| r34 真实用户链 | 源项目复制到新测试目录后，以 29 层、revision 0、安全系数 1.00 打开；分部响应草稿经返回主页和重开恢复，保存为 revision 1 后安全系数仍为 1.00；叠加证据与透明角色窗口均实际显示。源项目未改写。 |
| Agent 校准与迁移接口 | `describe --layer --revision` 返回源路径、透明连通区域、完整顶点基准/当前/delta、三角形和七通道权重；测试只使用该输出构造稀疏补丁并成功校准。`migrate` 用同一 PSD 创建独立新项目，按纹理指纹证明全部映射为 exact，旧项目未覆盖。 |
| 动态 revision 证据 | 公开 `record` 对 fixture 和 r34 均完成 autonomous/secondary 透明 WebM、局部视频、接触表和 JSON 报告；r34 revision 0 指纹一致，自主最大帧差 9.12%，secondary 最大帧差 4.51%，且头、身体、视线、呼吸、眨眼和嘴部极值全部为 0。重复输出被拒绝，非方形项目窗口与透明留边视频均保持 1.5:1。 |
| WebGL 视觉检查 | 透明像素 57.37%，可见像素 42.46%，两个确定性头部姿态之间有 13.22% 像素发生实际变化；Alpha、模板缓冲与透明背景均通过。 |
| 1280×1280/23 层性能 | 鼠标跟随和多段物理链开启时平均 60.004 FPS，95 分位 17.2 ms，WebGL2 可用。 |

性能数值是本次验收记录，不作为其它硬件的保证；自动脚本仍按 `docs/TESTING.md` 中的阈值重新测量。

## 本机开源样例

以下素材只从用户已经下载的压缩包解出到 `D:\Tools\PuppetLoomIntegration\run-20260814-2115`，没有提交到 PuppetLoom：

| 来源 | 输入 | 识别结果 | 最终结果 |
| --- | --- | --- | --- |
| Anime2.5DRig | 1280×1280、22 个可见图层 | `grouped` | 13 姿态通过、纹理齐全、透明窗口可运行 |
| AutoLive2d | 768×768、20 个可见图层 | `semantic` | 13 姿态通过、纹理齐全、透明窗口可运行 |

两者的安全缩放均为 `1`。这项检查证明外部 PSD 能走通导入、创建、报告、验证和真实 Electron 播放链；它不替代新角色原图与对应 See-through PSD 的最终视觉验收。

## 当前角色的本机复验

再次修复 `headwear` 后的 See-through PSD 为 1280×1280、24 个可见图层，24 层全部识别。当前 r34 从新版 PSD 重建，没有复用旧头饰纹理；`coherent-v3` 自动定位 23 个控制点，综合置信度为 `0.898667`，检测并把偏离的鼻点修正到脸部中心约束区。脸部与头骨作用图层均写入关键点报告；语义项目最大俯仰角为 `0.32 rad`，13 个姿态复验得分仍全部为 `1`，安全缩放保持为 `1`，没有靠缩小转头范围避开问题。上下观察只使用表面横轴旋转和深度差，不附加整头纵向位移；靠前五官的变化大于脸缘。脖子下端和衣领随头部在同一时刻进入缩幅后的上半身姿态，不再等待第二套慢响应。接入闭眼与三态嘴形后共 29 个运行图层，自动眨眼和短促说话开合均启用。七档姿态图包含左右两档、正面、抬头和低头；眼眶中心跟随脸部球面，原眼形再按远近缩放，因此近侧眼略宽、远侧眼明显收窄。脸侧前发的横向连接保留中立间距的约 70%–135%，垂直方向仍由头骨球面控制，不会穿进脸，也不会整片吸附。脖子上端跟头、下端跟同步运动的衣领，脸、前发、后发和上身没有断开。合并的 `headwear` 另推导出左右耳根固定点 `(0.449758, 0.153125)` 与 `(0.540086, 0.153125)`，分别驱动两侧耳翼，中央头饰不跟随耳朵弹动。前发 Alpha 分析把呆毛根定位在 `(0.494922, 0.076563)`，把刘海根定位在 `(0.494922, 0.134375)`，两点之间作为头皮保护区；前发网格相应提升为 `12×18`。

26 秒完整时间线测得：双铰链耳尖约 `20.86 px`、后发自由端约 `49.66 px`、前发与呆毛自由端约 `15.74 px`、裙摆约 `23.20 px`、上衣约 `0.61 px`、尾端沿圆弧最大移动约 `93.12 px`。裙摆横向峰值为 `19.39 px`，比上一版收小约 12%；后发提高刚度和驱动频率，回摆更快但仍保留五段链的根梢传递。尾巴所有网格点到根部的最大半径漂移为 `0 px`，因此不是纵向平移、缩放或伸缩。前发左右使用四段链，后发左右、呆毛和尾巴使用五段链；前发头皮保护区测得位移为 `0 px`，刘海和呆毛自由端仍能运动。把头部、身体、视线、呼吸和表情全部冻结后的预览仍能看到各部件独立风动；耳朵每次连续抬落三至四次后回中，呆毛强弹与常态摆动分开。脚部不参与身体摆动。耳朵定格表证明两个钉点保持不动；全身定格表证明裙摆左右和尾巴上下均能到达两个方向，同时尾巴长度保持不变。角色项目和素材只保存在忽略的本机 `workspace`，没有提交到 Git。
