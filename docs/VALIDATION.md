# 验收记录

日期：2026-08-15
平台：Windows，RTX 2080 Super

## 自动链

| 检查 | 结果 |
| --- | --- |
| 严格 TypeScript 检查 | core、renderer、cli、desktop 全部通过 |
| 构建 | 四个工作区全部通过 |
| 单元与集成 | 8 个测试文件、66 项测试全部通过，包含双表面头部、眼眶共用固定点、远近眼透视、脸颊与下巴转向、自然闭眼收拢、三态嘴形淡化、偶发完整开合、呆毛与裙摆分区、头饰与耳朵分向、前后发反向响应、尾根固定、素材参考裁切、视线回中、头发轮廓、中立还原、颈部下端固定和逐帧平滑检查 |
| Electron 用户链 | 创建、透明窗口、WebGL2、缩放、暂停、置顶、鼠标穿透恢复和重复启动复用同一窗口通过 |
| WebGL 视觉检查 | 透明像素 56.95%，可见像素 42.84%，相隔 1.8 秒两帧变化 11.55% |
| 1280×1280/23 层性能 | 平均 60.00 FPS，95 分位 16.8 ms，WebGL2 可用 |

性能数值是本次验收记录，不作为其它硬件的保证；自动脚本仍按 `docs/TESTING.md` 中的阈值重新测量。

## 本机开源样例

以下素材只从用户已经下载的压缩包解出到 `D:\Tools\PuppetLoomIntegration\run-20260814-2115`，没有提交到 PuppetLoom：

| 来源 | 输入 | 识别结果 | 最终结果 |
| --- | --- | --- | --- |
| Anime2.5DRig | 1280×1280、22 个可见图层 | `grouped` | 13 姿态通过、纹理齐全、透明窗口可运行 |
| AutoLive2d | 768×768、20 个可见图层 | `semantic` | 13 姿态通过、纹理齐全、透明窗口可运行 |

两者的安全缩放均为 `1`。这项检查证明外部 PSD 能走通导入、创建、报告、验证和真实 Electron 播放链；它不替代新角色原图与对应 See-through PSD 的最终视觉验收。

## 当前角色的本机复验

再次修复 `headwear` 后的 See-through PSD 为 1280×1280、24 个可见图层，24 层全部识别。r19 完全从新版 PSD 重建，没有复用 r18 的头饰纹理；`coherent-v2` 保持 `semantic` 绑定和完整动作范围，13 个姿态用当前变形代码复验均得分 `1`。接入闭眼与三态嘴形后共 29 个运行图层，自动眨眼和偶发嘴部开合均启用。同一只眼的眼白、虹膜、睫毛和闭眼层已统一固定点，五档左右转头图检查到 `0.85` 幅度均未再出现独立眼白尖角。26 秒、60 Hz 次级运动报告测得：头饰与耳尖最大 `1.586 px`、后发自由端 `23.232 px`、前发与呆毛 `4.247 px`、裙摆 `2.775 px`、上衣 `0.265 px`、尾端 `18.521 px`；根部保持在零或低位移，各部件由不同方向、频率与延迟驱动，脚部仍不参与呼吸。该角色项目和素材只保存在忽略的本机 `workspace`，没有提交到 Git。
