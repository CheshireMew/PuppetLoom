# 验收记录

日期：2026-08-16
平台：Windows，RTX 2080 Super

## 自动链

| 检查 | 结果 |
| --- | --- |
| 严格 TypeScript 检查 | core、renderer、cli、desktop 全部通过 |
| 构建 | 四个工作区全部通过 |
| 单元与集成 | 11 个测试文件、85 项测试全部通过，包含系统光标四向映射、角色脸部观察原点、视线先行/头部追随/身体延迟、横轴俯仰方向与表面深度差、颈部到衣领的连续连接、鼠标跟随退出平滑性、23 点自动定位、点位顺序与包含关系修正、脸部/头骨三角控制笼、极限转头和俯仰下的三角方向、实际作用图层报告、双表面头部、眼眶共用固定点、远近眼透视、脸颊与下巴转向、头颈连续连接、上半身横向及纵向跟随、自然闭眼收拢、三态嘴形淡化、偶发完整开合、多段物理链的根梢延迟与回正、左右头发独立响应、冻结主运动后的持续风动、呆毛二维弹跳、合并头饰的双耳钉点与快速弹动、尾根固定、素材参考裁切、视线回中、中立还原和逐帧平滑检查 |
| Electron 用户链 | 创建、透明窗口、WebGL2、系统光标目标、鼠标跟随开关、缩放、暂停、置顶、鼠标穿透恢复和重复启动复用同一窗口通过 |
| WebGL 视觉检查 | 鼠标停在固定位置时，透明像素 56.14%，可见像素 43.65%，相隔 1.8 秒两帧有 10.79% 的呼吸和次级运动变化 |
| 1280×1280/23 层性能 | 鼠标跟随和多段物理链开启时平均 59.639 FPS，95 分位 16.8 ms，WebGL2 可用 |

性能数值是本次验收记录，不作为其它硬件的保证；自动脚本仍按 `docs/TESTING.md` 中的阈值重新测量。

## 本机开源样例

以下素材只从用户已经下载的压缩包解出到 `D:\Tools\PuppetLoomIntegration\run-20260814-2115`，没有提交到 PuppetLoom：

| 来源 | 输入 | 识别结果 | 最终结果 |
| --- | --- | --- | --- |
| Anime2.5DRig | 1280×1280、22 个可见图层 | `grouped` | 13 姿态通过、纹理齐全、透明窗口可运行 |
| AutoLive2d | 768×768、20 个可见图层 | `semantic` | 13 姿态通过、纹理齐全、透明窗口可运行 |

两者的安全缩放均为 `1`。这项检查证明外部 PSD 能走通导入、创建、报告、验证和真实 Electron 播放链；它不替代新角色原图与对应 See-through PSD 的最终视觉验收。

## 当前角色的本机复验

再次修复 `headwear` 后的 See-through PSD 为 1280×1280、24 个可见图层，24 层全部识别。r27 完全从新版 PSD 重建，没有复用旧头饰纹理；`coherent-v3` 自动定位 23 个控制点，综合置信度为 `0.898667`，检测并把偏离的鼻点修正到脸部中心约束区。脸部与头骨作用图层均写入关键点报告；语义项目最大俯仰角为 `0.32 rad`，13 个姿态复验得分仍全部为 `1`，没有缩小动作范围。上下观察只使用表面横轴旋转和深度差，不再附加整头纵向位移；靠前五官的变化大于脸缘，脖子下端保持在衣领，上半身以较小比例延迟跟随俯仰。接入闭眼与三态嘴形后共 29 个运行图层，自动眨眼和偶发嘴部开合均启用。五档转头图包含延迟后的上半身姿态；眼角和嘴角作为真实控制笼顶点，同一眼眶的图层保持对齐，脖子上端跟头、下端跟衣领，脸、前发、后发和上身没有在 `0.85` 姿态断开。合并的 `headwear` 另推导出左右耳根固定点 `(0.449758, 0.153125)` 与 `(0.540086, 0.153125)`，分别驱动两侧耳翼，中央头饰不跟随耳朵弹动。

26 秒完整时间线测得：双铰链耳尖约 `20.86 px`、后发自由端约 `27.24 px`、前发与呆毛约 `20.70 px`、裙摆约 `7.19 px`、上衣约 `0.70 px`、尾端约 `12.76 px`。前发左右使用四段链，后发左右、呆毛和尾巴使用五段链；根部区域位移均保持在约 `1.05 px` 以内，自由端延迟响应。把头部、身体、视线、呼吸和表情全部冻结后的 12 秒预览仍能看到各部件独立风动；耳朵每次连续抬落三至四次后回中，呆毛强弹与常态摆动分开。脚部不参与身体摆动。耳朵定格表证明两个钉点保持不动，每次抬落都围绕独立转轴发生。角色项目和素材只保存在忽略的本机 `workspace`，没有提交到 Git。
