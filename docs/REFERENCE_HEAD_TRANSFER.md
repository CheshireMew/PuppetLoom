# 参考头部关键形的来源与职责

**最新结论（2026-09-09，用户反馈后重新对照）：revision 13 的参考转头候选视觉验收不通过。** 先前将静态九向、无翻折和抽帧结果当作动态效果合格，判断有误。以下内容保留实验过程，不能作为推荐此转头方案的结论。当前候选 revision 14 恢复 revision 0 的转头造型，保留几何眨眼、眼白网格、补充素材和惯性配对修复。

重新调用 PSD2Live 实际运行时输出17个左右中间姿态、17个上下中间姿态，与 revision 0、13 输入同样角度，见 jelly-audit。revision 13 脸部在左右半程相对参考的最大采样误差约0.016像素，上下半程约0.0001像素：误差证据不支持“只需加密关键帧就能修好”的猜测。当前角色的脸颊/下巴形态变化与原版相比过强，4秒内yaw一圈、pitch两圈的组合轨迹又放大了摆动；参考来源不等于更好的视觉效果。revision 14 在原组合轨迹的96个姿态中，脸部顶点与原版差异为0。旧参考关键形仍可从13版历史读取，未删除研究文件，也未撤销其它已做功能。

revision 14 已保存并重载，96帧组合翻折为0，几何眨眼绑定保持不变。逐帧对照图 all-frames-0/1/2 共覆盖全部96帧；before-after-same-motion.mp4 左侧为失败的13版、右侧为14版，输入轨迹、帧率及播放时长完全一致，未靠放慢或减少动作掩盖问题。另生成 runtime-r14 的8秒192帧桌面录像。这是恢复原有转头质量并保留其它修复，不宣称本次参考头脸迁移取得了提升。

2026-09-09。遵循用户最新要求：保留此前修改；采用实际验证过的参考能力，不自行扩展参考没有解决的问题。脖子等未获参考效果证明的问题不再作为新增改造目标。

在同一个猫娘 PSD 上调用 PSD2Live portable 0.4.0 的实际网格求值，提取 AngleX=-45/0/45、AngleY=-30/0/30 的九向网格。工具为研究目录中的 ReferenceGeometry.java，数据为 reference-geometry。参考 world Y 向上，转换到项目画布 Y 向下；按中立源网格三角形采样位移，保留当前中立绘画，不直接复制顶点编号。

最初的参考增量加程序投影试算在前发/耳部出现翻折，未提交该试算。原因是完整参考位移与本项目投影重复作用，而且带约束的投影不保证能反求。现通过公开 author apply 操作 set-layer-head-pose（mode=keyforms）把图层左右与上下转动交给完整关键形，跳过程序化 yaw/pitch；headRoll、表情、视线和次级运动仍各自生效。旧图层没有这个字段时保留原路径。这是运行时两种互斥的姿态来源，不是两套转头叠加，也不是把参考程序作为产品依赖。

候选从 revision 6 到 7（session 0007-cdf556f2-8a5f-4319-ad7b-9889ee00f675）保存17层九向关键形。已查看 reference-transfer-keyforms/sheet.png，13项静态安全姿态通过，参考采样目标与新运行时端点位置误差小于1e-10像素。此误差衡量转移实现一致性，不是视觉质量分数。

用户指出不能留下重复来源后，进一步合并16个旧的同轴纯几何绑定，保留同一主关键形的最终位移，保存为候选 revision 8。35组端点及半程坐标对照的最大差异约3.2e-13像素；表情和物理绑定没有合并或删除。旧版本保留在历史中。相关补丁、报告及合并脚本均位于 workspace/analysis/live2d-capability-20260907/reference-transfer-keyforms（脚本 consolidate-head.mjs 位于其父目录）。

目前判断仅针对已查看的九向静态：参考的侧转形态更清楚，因此候选采用参考关键形。连续过渡、眨眼组合，以及参考里没有的补充张口图层尚未完成复核；不能据此声称全套角色制作完成，也不能直接让所有旧项目改用新路径。项目 Skill 的素材/关键形流程已同步说明互斥职责和同轴绑定合并。

后续组合检查发现并修复了前述阶段的遗漏：revision 8 的同轴合并包含了6个惯性直接输入绑定，虽然保持了当时的坐标输出，却掩盖了它们与延迟输出的配对职责。revision 9 将直接项恢复为延迟项的反向位移，平衡后残差为0；补充嘴图与两张备用闭眼图共享参考脸部位移，共20层使用关键形转头。不能再将“所有 yaw 绑定合并”作为通用做法。

运行时现在先计算局部表情，再采样唯一完整双参数头部关键形的网格表面；修复原先睁眼位移与闭眼增量相加导致的翻折。程序化头脸制作对这些图层明确返回应编辑现有关键形，前发检查也改为读取实际关键形结果。revision 10 更新闭合端点；中立闭眼仍存在的眼白翻折追溯到不规则轮廓三角网，按参考的规则列网格处理，经两次单层 calibrate 保存到 revision 11/12。原中立局部眼白修形恢复为原纹理的规则映射，已经查看 eye-grid-neutral.png，不能描述为中立像素完全不变。其他表情绑定与头部关键形已重投影。

这些修复使用项目公开 author/calibrate 入口保存，研究脚本只负责来源采样和生成补丁。尚未把“从另一软件输出自动迁移整套角色”建设成通用产品功能，不应将此次单角色验证称为新角色自动制作全面完成。

revision 12 的96帧组合中，眼白翻折已经消除，剩余左右睫毛各一个不足1平方像素的翻折。revision 13 分别将左/右睫毛闭合位移调整到98.5%/99%，按同一轨迹复查。最后的组合证据在 combinations-r13；桌面运行时6秒、24fps的实际录像在 runtime-r13。组合轨迹覆盖 yaw 正负全程、pitch 正负0.8、闭眼及张口；桌面自主录像的 pitch 幅度很小，不能替代该组合轨迹的上下转头检查。闭眼端点微调属于已有参考曲线的网格适配，不是新增造型目标。

最终复查：revision 13 的96帧组合坐标全部有限，翻折为0；已查看组合抽帧图，并生成 turn-blink-mouth.mp4。桌面录像144帧成功，已查看每秒4帧的24图序列。相关 authoring/model/deform/primary/front-hair/eye-closure 测试通过，项目 Skill 的46项检查通过。眼白规则网格的中立前后差异共458个颜色通道，整图平均绝对通道差为0.000619（0..255量级）；变化很小，但不是像素完全一致。
